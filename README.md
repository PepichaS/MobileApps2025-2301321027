# Just Commit.

## 📌 Идея

Мобилно приложение за изграждане на навици чрез „commit“ система:

1. Задаваш си цел – брой последователни дни
2. Всеки ден „комитваш“ (отбелязваш изпълнение)
3. Ако пропуснеш ден → streak-ът се нулира
4. Виждаш прогреса си в Dashboard, Calendar и Progress екрани
5. Можеш да добавяш photo + GPS proof и да активираш напомняния

---

## ⚙️ Как работи

- **Навици (Habits)** – всеки навик има: име, описание, цел (брой дни), история на изпълненията, текущ streak.
- **Streak логика** – streak-ът е броят последователни дни (до днес или до последния изпълнен ден). Пропуск занулява поредицата.
- **Commit за деня** – на екрана _Habit Details_ натискаш „Mark as Done“ / „I did this today“.
- **Доказателство (Proof)** – след като маркираш деня като изпълнен, можеш да добавиш:
  - снимка (camera intent през `expo-image-picker`)
  - GPS локация (чрез `expo-location`)
- **Reminder Notifications** – по избор за всеки навик, един reminder час на ден (използва `expo-notifications`).
- **Календар (Calendar View)** – визуализира дни с изпълнени навици, intensity по брой навици + различен цвят за дни с proof и „perfect days“ (всички навици изпълнени).
- **Прогрес (Progress Screen)** – показва:
  - общ брой навици, общи изпълнения, best/average streak
  - процент изпълнение спрямо целите
  - разбивка по навик (progress bar + proof статистика)
  - **Achievements** – отключващи се постижения (напр. 7/21 дни streak, брой proofs, 100% completion).
- **UX екстри**:
  - onboarding банер за нови потребители
  - motivational header („Just Commit.“ с дневно съобщение)
  - празни/loading състояния с обяснителен текст

---

## 🧱 Архитектура

Основни модули и структура (съкратено):

- `src/app` – Expo Router навигация
  - `(tabs)/` – основните таб екрани: Dashboard, Calendar, Progress
  - `habit/add`, `habit/[id]` – добавяне и детайл на навик
- `src/resources/screens` – екрани (feature-level UI):
  - `Dashboard.tsx` – ежедневен изглед, бързо „mark as done“, summary карти, 100% celebration
  - `HabitDetails.tsx` – детайли за навик, streak, reminder, proof галерия, delete с AlertDialog
  - `Calendar.tsx` – календар с оцветени дни, списък с навици/доказателства за избран ден
  - `Progress.tsx` – обща статистика, per-habit progress, achievements
  - `AddHabit.tsx` – стъпково (guided) създаване на навик с пресети (7/21/30/66 дни)
- `src/resources/components/ui` – дизайн система:
  - `Button`, `Card`, `Text`, `Input`, `Alert`, `AlertDialog`, `Progress`, `Switch`, `Checkbox`, `Icon` и др.
- `src/resources/models/Habit.ts` – типове: `Habit`, `HabitReminder`, `HabitProof` и т.н.
- `src/resources/storage/HabitStorage.tsx` – работа с AsyncStorage (CRUD за навици + proof-ове).
- `src/resources/services/NotificationService.ts` – логика за reminder-и (request permissions, schedule/cancel).
- `src/resources/lib/theme.ts` – светла/тъмна тема, цветове за UI и navigation.

**Tech Stack (накратко):**

- Expo (SDK 54) + React Native
- Expo Router (file-based навигация)
- NativeWind (Tailwind класове за React Native)
- AsyncStorage за локално съхранение
- Expo Notifications, Image Picker, Location
- RN Primitives за изградени UI компоненти

---

## 📲 Потребителски поток

1. **Add Habit** – попълваш:
   - име на навик
   - по желание описание („защо“)
   - целеви брой дни (може да избереш preset – 7/21/30/66)
   - по желание: дневен reminder час
2. **Dashboard** – виждаш списък с навици за днес, текущ streak, брой дни към целта.
   - можеш да маркираш навик като изпълнен директно от списъка (чекбокс)
3. **Habit Details** – по-подробен изглед за конкретен навик:
   - „Mark as Done“ за днес + час на изпълнение
   - добавяне на **Photo + GPS proof**
   - редакция на име/описание/цел, управление на reminder
4. **Calendar** – визуализира дни с изпълнение:
   - по-наситен цвят = повече навици за деня
   - специален акцент за дни с proof / perfect days
   - списък с навиците и броя proofs за избрания ден
5. **Progress** – глобална картина на навиците:
   - общи статистики (total habits, total completions, best/avg streak)
   - общ процент спрямо всички цели
   - detail за всеки навик (progress bar + proof инфо)
   - **Achievements** – отключващи се/заключени постижения с прогрес ленти
6. **Дългосрочно** – streak-ове, achievements и визуално възнаграждаване мотивират потребителя да „Just Commit.“ всеки ден.

---

## ▶️ Стъпки за стартиране

```bash
cd src
npm install
npx expo start
```

- Избери платформа (iOS simulator, Android emulator или Expo Go на реално устройство).
- Увери се, че си дал разрешения за **камера**, **локация** и **нотификации**, за да работят proof-овете и reminder-ите.

---

## 👤 Тестови акаунти

Приложението работи изцяло **локално** (AsyncStorage), без бекенд и без login система.

- Няма предварително дефинирани тестови акаунти.
- Всеки инсталирал приложението започва със „чист“ локален сторидж и може да си създаде собствени навици за тестване.

---

## 📦 APK

Готов APK файл се поддържа директно в репото в:

```text
src/apk/
```

Примерно име (препоръка): `just-commit-preview.apk`

### Инсталация на телефона

1. Копирай APK файла от `src/apk/` на Android устройството (USB, AirDrop-alternative, Google Drive и т.н.).
2. На телефона позволи инсталация от „неизвестни източници“ за приложението, с което отваряш файла (Files / браузър и т.н.).
3. Отвори APK файла на устройството и потвърди инсталацията.
